import React from "react"; import { StackLogger as Core, type StackLoggerConfig } from "@stacklogger/core";
export const StackLogger = Core;
export function installBrowserCapture() { if (typeof window === "undefined") return () => { }; const onError = (e: ErrorEvent) => Core.captureException(e.error ?? e.message, { handled: false }); const onReject = (e: PromiseRejectionEvent) => Core.captureException(e.reason, { handled: false }); window.addEventListener("error", onError); window.addEventListener("unhandledrejection", onReject); return () => { window.removeEventListener("error", onError); window.removeEventListener("unhandledrejection", onReject) } }
type Props = { children: React.ReactNode; fallback: React.ReactNode | ((p: { error: Error; reset: () => void }) => React.ReactNode); onError?: (error: Error, info: React.ErrorInfo) => void };
export class StackLoggerErrorBoundary extends React.Component<Props, { error: Error | null }> { state = { error: null }; static getDerivedStateFromError(error: Error) { return { error } } componentDidCatch(error: Error, info: React.ErrorInfo) { Core.captureException(error, { handled: false, contexts: { react: { componentStack: info.componentStack } } }); this.props.onError?.(error, info) } render() { if (this.state.error) return typeof this.props.fallback === "function" ? this.props.fallback({ error: this.state.error, reset: () => this.setState({ error: null }) }) : this.props.fallback; return this.props.children } }
export type { StackLoggerConfig };
